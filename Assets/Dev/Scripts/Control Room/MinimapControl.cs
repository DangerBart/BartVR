using System.Collections; using System.Collections.Generic; using UnityEngine; using UnityEngine.UI;  public class MinimapControl : MonoBehaviour {      [SerializeField]     private GameObject defaultMarker;      // Use this for initialization     void Start () {              }        // Update is called once per frame     void Update () {             }      public void SetNotificationMinimapLocation (Notification notification)     {         GameObject marker = Instantiate(defaultMarker) as GameObject;         marker.SetActive(true);     } } 